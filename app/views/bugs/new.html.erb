<%= render 'shared/content_title', title: '提交bug' %>

<div class="row text-center">
  <p><i class="fa fa-bug fa-4x"></i></p>
</div>

<div class="well">
  <%= form_for @bug, :html => {class: 'form-horizontal'} do |f| %>
    <div class="form-group">
      <%= f.label :标题, class: 'col-sm-2 control-label' %>
      <div class="col-sm-10">
      <%= f.text_field :title, class: 'form-control', autofocus: true %>
      </div>
    </div>
    <div class="form-group">
      <%= f.label :内容, class: 'col-sm-2 control-label' %>
      <div class="col-sm-10">
      <%= f.text_area :description, class: 'form-control' %>
      </div>
    </div>
    <div class="form-group">
      <div class="col-sm-offset-2 col-sm-10">
        <%= f.submit(@bug.new_record? ? "提交" : "更新", class: 'btn btn-default') %>
      </div>
    </div>
  <% end %>
</div>